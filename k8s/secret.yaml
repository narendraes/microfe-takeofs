apiVersion: v1
kind: Secret
metadata:
  name: microfe-takeofs-secrets
type: Opaque
data:
  # Base64 encoded values
  # Example: echo -n "your-api-key" | base64
  PRODUCTION_API_KEY: ${BASE64_ENCODED_API_KEY}
  # Add other sensitive environment variables here 